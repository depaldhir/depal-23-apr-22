# Create file called `makefile` and copy the `contents` to it

# How to execute

* Run all machine create related tasks
  `make name=<vm-name>`

* Run all delete related tasks
  `make delete`

* Run specific tasks
  `make shutdown` 
  `make reboot`
